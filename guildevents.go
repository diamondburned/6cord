package main

import "github.com/rumblefrog/discordgo"

func guildCreate(s *discordgo.Session, gc *discordgo.GuildCreate) {
	onReady(s, &d.State.Ready)
}

func guildDelete(s *discordgo.Session, gc *discordgo.GuildCreate) {
	onReady(s, &d.State.Ready)
}
